import * as React from "react";
import { View, Text } from "react-native";

export function CreateAlertScreen() {
    return (
        <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
            <Text style={{ fontSize: 22 }}>Create Alert Screen</Text>
        </View>
    );
}